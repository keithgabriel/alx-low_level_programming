#include "main.h"

/**
 * _indigit - function checks for a digit (0 through 9)
 * @c: integer to be checked
 * Return: 1 if c is a digit, 0 otherwise
 */
int _indigit(int c)
{
return (c >= '0' && c <= '9');
}
